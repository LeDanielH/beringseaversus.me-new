var distJekyll = '_site',
	production = 'prod';
    srcSass = '_pages/**/*.+(scss|sass)';
    distSass = '_pages/';

var paths = {
	// html: {
	// 	src: src + '/**/*.html',
	// 	dist: dist + '/'
	// },
	stylesMine: {
        src: srcSass,
        dist: distSass
    },

    stylesLibs: {
        src: '',
        dist: ''
    },

    scriptsMine: {
        src: src + '/scripts/',
        dist: dist + '/scripts/'
    },

    scriptsLibs: {
        src: '',
        dist: '' 
    },

    images: {
        src: src + '/data/images/**/*',
        dist: dist + '/data/images/'
    },
    fonts: {
        src: 'bower_components/**/*.+(eot|ttf|woff|woff2)',
        dist: dist + '/styles/fonts/'
    },
    bower: 'bower_components/',
    deploy: './' + dist + '/**/*',
};

var jsLibraries = {
    jquery: {
        min: paths.bower + 'jquery/dist/jquery.min.js',
        debug: paths.bower + 'jquery/dist/jquery.js'
    },
    semanticUI: {
        min: '',
        debug: paths.bower + 'semantic-ui-sass/app/assets/javascripts/semantic-ui.js'
    }
};

var renderedNames = {
	javascript: {
		myJs: 'my-scripts.js',
		debug: 'libraries-debug.js',
		min: 'libraries.min.js'
	}
};

var ignoreModernizr = '!' + paths.scripts.src + 'autogenerated/modernizr.js';
var watchedJsFiles = [ignoreModernizr, paths.scripts.src + '**/*.js'];

module.exports = {
    paths: paths,
    jsLibraries: jsLibraries,
    ignoreModernizr: ignoreModernizr,
    watchedJsFiles: watchedJsFiles,
    renderedNames: renderedNames,
};